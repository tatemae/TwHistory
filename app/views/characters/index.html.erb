<h1>Historical Figures for <%= @project.title %></h1>
<%= link_to '<< Back', @project %>
<%= link_to translate('projects.add'), new_project_character_path(@project) if @project.can_edit?(current_user) %>
<div>
  <%= render @characters %>
</div>