<%= f.text_field :name, { :label => translate('projects.title') } -%>
<%= f.text_area :bio, { :label => translate('projects.description') } -%>
<%= image_tag @character.photo.url(:thumb) if @character && @character.photo_file_name %>
<%= f.file_field :photo, { :label => 'Upload a Photo (Please use a square image)' } %>