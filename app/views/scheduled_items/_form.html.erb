

<%= form_for(@scheduled_item) do |f| %>
  <% if @scheduled_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scheduled_item.errors.count, "error") %> prohibited this scheduled_item from being saved:</h2>

      <ul>
      <% @scheduled_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
