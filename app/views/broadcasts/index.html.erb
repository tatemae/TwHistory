<h2>Broadcast Schedule</h2> 
<h4>Re-enactments in Progress</h4>
<% if @broadcasts_in_progress.any? -%>
  <ul class="progress-list">
    <%= render :partial => 'broadcasts/broadcast', :collection => @broadcasts_in_progress %>
  </ul>
<% else -%>
  No current broadcasts
<% end -%>
<h4>Scheduled Re-enactments</h4> 
<div id="calendar"></div>

<script type="text/javascript" language="JavaScript">
  jQuery(document).ready(function(){
  	var date = new Date();
  	var d = date.getDate();
  	var m = date.getMonth();
  	var y = date.getFullYear();
	
  	jQuery('#calendar').fullCalendar({
  		header: {
  			left: 'prev',
  			center: 'title',
  			right: 'next'
  		},
  		buttonText: {
          	prev: '&nbsp;',
          	next: '&nbsp;'
  	    },
  		editable: false,
  		events: <%= @broadcasts.to_json(:only => [:id], :methods => [:title, :start, :end]).html_safe -%>
  	});	
  });
</script>